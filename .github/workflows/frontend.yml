name: Frontend CI

on:
  push:
    branches:
      - main
      - master
      - "fix/**"
      - "feat/**"
      - "ci/**"
    paths:
      - "MURA-app/**"
      - ".github/workflows/frontend.yml"
  pull_request:
    paths:
      - "MURA-app/**"
      - ".github/workflows/frontend.yml"

jobs:
  test:
    name: Test & Build Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: MURA-app

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "MURA-app/package-lock.json"

      - name: Refresh security lockfile
        run: |
          npm install --package-lock-only
          npm audit fix --package-lock-only --omit=dev

      - name: Save refreshed lockfile
        uses: actions/upload-artifact@v4
        with:
          name: refreshed-frontend-lock
          path: MURA-app/package-lock.json
          if-no-files-found: error

      - name: Install dependencies
        run: npm ci

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: ESLint
        run: npm run lint

      - name: Typecheck TypeScript
        run: npx tsc --noEmit

      - name: Run unit & integration tests
        run: npm test -- --run

      - name: Production build
        run: npm run build
